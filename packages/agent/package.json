{
    "name": "@arpa/agent",
    "version": "0.1.0",
    "type": "module",
    "scripts": {
        "dev": "bun run --watch src/index.ts",
        "build": "bun build src/index.ts --outdir=dist --target=bun",
        "test": "bun test",
        "typecheck": "tsc --noEmit"
    },
    "dependencies": {
        "@arpa/backend": "workspace:*",
        "zod": "4.0.17"
    },
    "devDependencies": {
        "@types/bun": "1.2.20"
    }
}
